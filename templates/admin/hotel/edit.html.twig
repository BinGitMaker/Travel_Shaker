{% extends 'admin/base.html.twig' %}

{% block title %}Edit Hotel{% endblock %}

{% block body %}
<h1 class="d-flex justify-content-center text-secondary">{{ hotel.name }}</h1>
<a href="{{ path('admin_hotel_index') }}">Retour à la liste</a>
<section class="mx-5"></section>
    {{ include('admin/hotel/_form.html.twig', {'button_label': 'Valider'}) }}
</section>

    {{ include('admin/hotel/_delete_form.html.twig') }}
{% endblock %}
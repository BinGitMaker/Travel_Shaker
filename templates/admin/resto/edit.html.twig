{% extends 'admin/base.html.twig' %}

{% block title %}Edit Resto{% endblock %}

{% block body %}
    <h1>Edit Resto</h1>

    {{ include('admin/resto/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_resto_index') }}">back to list</a>

    {{ include('admin/resto/_delete_form.html.twig') }}
{% endblock %}

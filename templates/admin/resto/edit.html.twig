{% extends 'admin/base.html.twig' %}

{% block title %}Edit Resto{% endblock %}

{% block body %}
<h1 class="d-flex justify-content-center text-secondary">{{ resto.name }}</h1>
<a href="{{ path('admin_resto_index') }}">Retour à la liste</a>
<section class="mx-5"></section>
    {{ include('admin/resto/_form.html.twig', {'button_label': 'Valider'}) }}
</section>
    
    {{ include('admin/resto/_delete_form.html.twig') }}
{% endblock %}
